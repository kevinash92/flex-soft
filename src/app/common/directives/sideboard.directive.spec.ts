import { SideboardDirective } from './sideboard.directive';

describe('SideboardDirective', () => {
  it('should create an instance', () => {
    const directive = new SideboardDirective();
    expect(directive).toBeTruthy();
  });
});
